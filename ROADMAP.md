# GameLog 路线图（ROADMAP）

> 更新：2026-08-15（grilling 会话产出，beta 1.8.1 之后）
> 来源：按类似软件（Backloggd / Grouvee / HowLongToBeat / 游戏时光 / Steam Replay）设计经验逐轮收敛的共识。
> 状态：**近期路线图**，分阶段实施；实施顺序见下。实现时以本文件为准，细节在 HANDOVER.md 记录。

## 定位约束

- **北极星**：纯本地个人工具 · 极致打磨。不做社交 / 账号 / 公开主页，不做对外社区。
- **数据**：双端本地独立存储 + 备份互通（AirDrop / 文件导入），**不做云自动同步**（SwiftData+CloudKit 或 iCloud Drive 自动同步都需要 Apple Developer 账号 + entitlement，与「纯个人 + 近期可落地」矛盾，明确放弃；手动传输维持现状）。
- **分享图**：保留（作为个人记录的输出 / 纪念存档，不违背纯个人定位）。
- **粒度**：近期可落地的一批改进，小步迭代。

## 阶段 1 · 状态机（核心，优先做）

现状：`Game` 创建必带首条通关记录 + 六维评分，库 = 「已通关」单一视角。

- `Game` 加 `status` 字段：**想玩 / 在玩 / 搁置 / 弃坑 / 已通关**（5 状态）。
- **一等公民模型**：想玩 / 在玩 / 搁置 / 弃坑不需要通关记录和评分；流转到「已通关」才需要（挂现有记录）。
- **迁移**：现有 `Game` 默认 `status = completed`（已通关），SwiftData 增量迁移，零风险。
- **新建流程放开**：不再强制「首条记录 + 评分 + 评价标题」；新建可选状态，非已通关状态无记录/评分要求。
- **交互多入口**：详情页状态选择器 + 库卡片右键（macOS）/长按（iOS）快捷切换 + 搜索结果直接「+ 想玩」（想玩是高频动作，录入路径要短）。
- **库视图**：筛选菜单加「状态」维度（全部/想玩/在玩/搁置/弃坑/已通关），macOS 侧边栏 + iOS 筛选菜单同步；想玩清单 = 点一次筛选直达。
- **零记录游戏全链路审计**：`platformList` / `latestCompletionDate` / `libraryScore` / 分享图 / 统计 / 排序在无记录状态下不崩（现状删光记录已有 graceful 处理，需系统验证）。
- **统计口径**：想玩/在玩不计入「总通关数 / 库平均分」；统计页可加「想玩 N」计数。
- **备份**：`GameDTO` 加可选 `status`，旧备份缺省 = 已通关，向后兼容。

## 阶段 2 · 发售日自动填充

- 名字输入防抖（与「自动匹配封面」同一交互）自动查并填；编辑时自动补空白。
- 数据源：Steam 官方无 key 接口（`store.steampowered.com/api/appdetails`，已调研可行）；SteamGridDB 不返回发售日期；非 Steam 平台搜不到需手动兜底。

## 阶段 3 · 统计可视化 / 年度回顾

- 统计页扩充：月度热力、通关趋势、平台占比、时长分布。
- 年度回顾可生成分享图存档（复用分享卡渲染管线，ImageRenderer）。

## 阶段 4 · 封面缓存

- 仅内存 `NSCache` 缓存封面解码结果，长列表/网格滚动不重复解码。
- **视觉不变**（与用户此前「封面缩略图优化保留现状」的决策兼容——只缓存解码、不改缩略图 UI/尺寸）。

## 穿插 · iOS 真机 / iPad 适配补完

在每次改动后于真机验证：拍照（`UIImagePickerController`）、AirDrop 备份导入、iPad 布局、`QLPreviewController` 照片预览、PhotosPicker、TabBar 全流程。只有真机能完整验证。

## 明确不做

- 云自动同步（CloudKit / iCloud Drive）——需开发者账号 + entitlement，与定位矛盾
- 社交 / 账号 / 好友 / 公开主页 / 社区榜单
- 删除分享图
- 缩略图 UI / 尺寸优化（用户此前明确保留现状；封面缓存只做内存解码缓存，不触视觉）

## 遗留小决策（实现时定，不必现在）

- 想玩清单是否也出分享图（目前分享图面向已通关记录）
- 快捷录入（「+ 想玩」）的搜索数据源细节（SteamGridDB 现有搜索面板复用 vs 简化）
- 统计图表的精确形态（柱状 / 热力格子 / 饼图等）
